# Модерация и жалобы

## Жалобы на посты (`post_reports`)

### Куда попадает жалоба

1. Пользователь жмёт **Пожаловаться** в меню поста (`post.report`).
2. Сервер пишет строку в `post_reports` через **service role** (RLS для браузера закрыт).
3. Поля: `post_id`, `reporter_user_id`, `reason` (пока не спрашиваем в UI), `created_at`.
4. Повторная жалоба того же пользователя на тот же пост → ошибка «Вы уже отправили жалобу».

Админская очередь доступна на `/admin/moderation`. Модератор может:

- отклонить жалобу;
- открыть исходную публикацию;
- после отдельного подтверждения удалить публикацию и её загруженное медиа.

Решения пишутся в закрытый `admin_audit_log`. Жалоба сама по себе по-прежнему
не скрывает публикацию и не создаёт автоматических санкций.

### Последствия для пользователей (текущая версия)

| Что происходит | Что не происходит |
|----------------|-------------------|
| Жалоба сохраняется в БД и попадает в очередь | Пост не скрывается автоматически |
| Автор видит toast «Жалоба отправлена» | Автор поста не уведомляется |
| Один reporter — одна запись | Нет публичного счётчика жалоб |

Следующий этап: предупреждения, временные ограничения, апелляции и отдельные
роли модераторов. Бан по одной жалобе намеренно не автоматизируется.

### Запросы для разработки

```sql
-- последние жалобы с постом
SELECT r.*, p.author_id, p.text
FROM post_reports r
JOIN posts p ON p.id = r.post_id
ORDER BY r.created_at DESC
LIMIT 50;
```

Миграции: `drizzle/16-post-edit-reports.sql`, затем
`drizzle/26-admin-observability.sql`.

## Связанные доки

- [posts.md](./posts.md) — меню поста и API
- [security.md](./security.md) — RLS и service role
