/** Export size for custom banners — matches profile card visible ratio (see docs/customization.md). */
export const BANNER_EXPORT_WIDTH = 640;
export const BANNER_EXPORT_HEIGHT = 240;
export const BANNER_ASPECT_RATIO = BANNER_EXPORT_WIDTH / BANNER_EXPORT_HEIGHT;

/** Center band that stays visible under avatar on most card widths (~70% per spec). */
export const BANNER_SAFE_WIDTH_RATIO = 0.7;
